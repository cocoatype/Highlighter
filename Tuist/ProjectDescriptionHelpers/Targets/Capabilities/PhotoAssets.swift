import ProjectDescription

public enum PhotoAssets {
    public static let target = Target.capabilitiesTarget(
        name: "PhotoAssets",
        dependencies: [
        ]
    )

    public static let testTarget = Target.capabilitiesTestTarget(
        name: "PhotoAssets",
        dependencies: [
        ]
    )
}
