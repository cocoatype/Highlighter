disabled_rules:
  - line_length # too many violations already
  - shorthand_operator # disagree with this rule
  - identifier_name # incompatible with "name a variable" redemption
  - statement_position # doesn't work with expression syntax
  - type_name # same
excluded:
  - .build
  - Derived
  - vendor

cyclomatic_complexity:
  ignores_case_statements: true

trailing_comma:
  mandatory_comma: true
  
